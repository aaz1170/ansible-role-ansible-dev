---
- name: Converge
  hosts: all
  vars:
    developers:
      - vagrant
      - johndoe
      - janedoe
    pip_versions:
      ansible: "2.8.0"
      molecule: "2.20"
  roles:
    - role: ansible-role-ansible-dev
